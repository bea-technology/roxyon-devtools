---
---

M3 (docs layer, no package changes): added `docs/llms/{roxyon,lumenjs}.llms.txt`,
`docs/openapi/baas.yaml` (OpenAPI 3.1, validates with redocly), a filled-in-able
`docs/templates/AGENTS.md`, and `docs/DISTRIBUTION.md` (hosting + registry +
per-platform-wrapper checklist). Nothing to publish yet.
